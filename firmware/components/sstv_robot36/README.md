# `sstv_robot36` — placeholder

Phase 4 work. Planned content:

- Robot36 SSTV encoder (320×240 RGB → 16 kHz mono PCM)
- I²S output driver for the audio codec
- PTT GPIO control for the VHF transceiver
- IR-overlay compositor (picture-in-picture, RGB inset inside IR background)

See `PROJECT_MEMORY.md` section 4 for the audio path details. Empty register
for now.
